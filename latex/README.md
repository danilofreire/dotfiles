# LaTeX

Configuration for [latexmk](https://mg.readthedocs.io/latexmk.html), a build tool for LaTeX documents.

## Files

- `.latexmkrc` - latexmk configuration (PDF viewer, build options)

## Install

```bash
cp .latexmkrc ~/
```
