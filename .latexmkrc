# This file contains the configuration for the latexmk tool.
# The $clean_ext variable specifies the file extensions to be removed when running the 'clean' command.
$clean_ext = 'synctex.gz synctex.gz(busy) run.xml tex.bak bbl bcf fdb_latexmk run tdo xdv %R-blx.bib'